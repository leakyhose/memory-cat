export default function GameBoard (
    {
        currentScore,
        setCurrentScore,
        setScreenState
    }
){
    const [card, setCard] = setState("");
    const [seenCards, setSeenCards] = setState("");
    return <div>
        
    </div>
}